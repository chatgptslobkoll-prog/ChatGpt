<Window x:Class="Amonic.App.Views.CrashReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Причина сбоя"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="260" Width="520">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Предыдущий сеанс завершился некорректно. Укажите причину сбоя:" TextWrapping="Wrap" />
        <TextBox x:Name="ReasonTextBox" Grid.Row="1" Margin="0,8,0,0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <TextBlock x:Name="ErrorTextBlock" Grid.Row="2" Foreground="Firebrick" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="120" Margin="0,0,8,0" Content="Сохранить" Click="Save_Click"/>
            <Button Width="120" Background="IndianRed" Foreground="White" Content="Отмена" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>

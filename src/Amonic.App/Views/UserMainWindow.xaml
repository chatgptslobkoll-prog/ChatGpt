<Window x:Class="Amonic.App.Views.UserMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Главное меню пользователя"
        WindowStartupLocation="CenterScreen"
        Height="520" Width="920">
    <DockPanel Margin="12">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Выход" Click="Exit_Click"/>
        </Menu>

        <StackPanel>
            <TextBlock x:Name="WelcomeTextBlock" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="TimeTextBlock" Margin="0,8,0,0" />
            <TextBlock x:Name="CrashTextBlock" Margin="0,0,0,10" />

            <DataGrid x:Name="ActivityDataGrid" Height="380" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Вход" Width="*" Binding="{Binding LoginAt}"/>
                    <DataGridTextColumn Header="Выход" Width="*" Binding="{Binding LogoutAt}"/>
                    <DataGridTextColumn Header="Длительность" Width="120" Binding="{Binding Duration}"/>
                    <DataGridTextColumn Header="Причина сбоя" Width="2*" Binding="{Binding CrashReason}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </DockPanel>
</Window>

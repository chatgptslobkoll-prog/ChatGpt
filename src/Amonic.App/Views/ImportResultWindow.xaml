<Window x:Class="Amonic.App.Views.ImportResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результаты импорта"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="250" Width="430">
    <Grid Margin="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Импорт завершен" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
        <TextBlock x:Name="AddedTextBlock" Grid.Row="1" Margin="0,4"/>
        <TextBlock x:Name="OtherTextBlock" Grid.Row="2" Margin="0,4"/>
        <Button Grid.Row="3" Width="120" Height="30" HorizontalAlignment="Right" VerticalAlignment="Bottom" Content="ОК" Click="Ok_Click"/>
    </Grid>
</Window>

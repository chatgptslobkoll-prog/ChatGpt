<Window x:Class="Amonic.App.Views.ChangeRoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена роли"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="260" Width="430">
    <Grid Margin="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="Смена роли выбранного пользователя" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="Текущая:" VerticalAlignment="Center"/>
        <TextBlock x:Name="CurrentRoleTextBlock" Grid.Row="1" Grid.Column="1" Margin="0,4"/>

        <TextBlock Grid.Row="2" Text="Новая роль:" VerticalAlignment="Center"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="2" Grid.Column="1" Margin="0,4">
            <ComboBoxItem Content="Пользователь" Tag="2"/>
            <ComboBoxItem Content="Администратор" Tag="1"/>
        </ComboBox>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="120" Margin="0,0,8,0" Content="Применить" Click="Apply_Click"/>
            <Button Width="120" Background="IndianRed" Foreground="White" Content="Отмена" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
